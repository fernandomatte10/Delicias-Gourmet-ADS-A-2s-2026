// Importa os schemas do projeto
import {produtoType} from './produtoType'
import {categoriaType} from './categoriaType'

// Registra os schemas utilizados pelo Sanity Studio
export const schemaTypes = [
  produtoType,
  categoriaType,
]